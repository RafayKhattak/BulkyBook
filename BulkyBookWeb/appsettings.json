{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  /* Logging configuration specifying log levels for different categories */

  "AllowedHosts": "*",
  /* Allowing requests from any host (CORS policy) */

  "ConnectionStrings": {
    "DefaultConnection": "Server=(localdb)\\MSSQLLocalDB;Database=Bulky;Trusted_Connection=True;"
  }
  /* Connection string for the default database named "Bulky" on the local SQL Server instance */
}
